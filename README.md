# what is ULTRA-RANDOM-X.js?

`ultra-random-x.js` is a node library for create a **real** random numbers.

# install
```bash
npm install ultra-random-x.js
```

# Example
```javascript
import { randomInt } from "ultra-random-x";

console.log(randomInt(1, 100));
```
